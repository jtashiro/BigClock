# Homebrew API package


This directory contains classes to fetch, hold and map weather data from the local homebrew python DreamScreenWeather python app the runs on local unix installation

** This will be decommed in favor of external providers

